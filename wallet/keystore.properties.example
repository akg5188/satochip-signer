# Optional: copy to keystore.properties if you want release APKs signed
# with your own long-lived keystore instead of the default debug keystore.
storeFile=/absolute/path/to/your-release.keystore
storePassword=change-me
keyAlias=release
keyPassword=change-me
